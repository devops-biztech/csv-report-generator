# Database Configuration (SQLite — local file)
# Path to the SQLite file; the `file:` prefix is optional and the directory is
# created automatically. Defaults to ./data/demo.db when unset.
DATABASE_URL=file:./data/demo.db

# No auth and no external services — this app is entirely local.
